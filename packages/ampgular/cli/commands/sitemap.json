{
  "$schema": "http://json-schema.org/schema",
  "$id": "amp-cli://commands/sitemap.json",
  "description": "The spider command crawls your entire site preparing the routes paths for later pre-render or ssr",
  "$longDescription": "./sitemap-long.md",

  "$aliases": ["d"],
  "$scope": "in",
  "$type": "sitemap",
  "$impl": "./sitemap-impl#SitemapCommand",
  "allOf": [
    { "$ref": "./definitions.json#/definitions/base" }
   
  ]
}
